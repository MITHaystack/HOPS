This directory is to serve as a place to collect external 3rd party (primarily HEADER-ONLY) libraries.
Each library should get its own sub-directory either as a git submodule or as version-fixed frozen copy. 
