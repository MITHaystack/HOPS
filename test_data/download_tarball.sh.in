#!/bin/bash
DATA_TARBALL=@CMAKE_CURRENT_BINARY_DIR@/hops4_testdata.tar.gz

if [ -f "$DATA_TARBALL" ]; then
    echo "$DATA_TARBALL exists."
else 
    #wget -q ftp://gemini.haystack.mit.edu/pub/barrettj/hops/hops4_testdata.tar.gz -O $DATA_TARBALL
    wget -q http://www.mit.edu/~barrettj/hops/hops4_testdata.tar.gz -O $DATA_TARBALL
fi

if [ -f "$DATA_TARBALL" ]; then
    echo "0"
else
    echo "1"
fi
